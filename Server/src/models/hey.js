// const mongoose = require('mongoose');
// import { Keep } from './Keep.model';
// const theNewKeepsSchema = Keep.discriminator(
//   'hey',
//   new mongoose.Schema({ pin: Boolean }, options)
// );
